[project]
name = "pi-health-checks-dev"
version = "0.0.0"
description = "Dev tooling shim — declares shellcheck-py for uv tool run."
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "shellcheck-py>=0.10.0.1",
]

[tool.uv]
package = false
